name: ansible-lint
on:
  push:

jobs:
  build:
    name: Ansible Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: run-ansible-lint
        uses: ansible/ansible-lint@main